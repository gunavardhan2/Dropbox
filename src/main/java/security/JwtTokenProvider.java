package security;

public class JwtTokenProvider {
    public <Authentication> void authenticateUser(String username, String password) {
    }

    public String generateToken(Authentication authentication) {
    }

    public <Authentication> String generateToken(Authentication authentication) {
        return null;
    }
}
